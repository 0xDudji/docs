---
title: "Achieved"
description: "Certifications I have already achieved."
---

## Earned Certifications

<div style={{ display: "flex", flexDirection: "column", gap: "0.9rem" }}>
  <div style={{ border: "1px solid var(--border)", borderRadius: "14px", overflow: "hidden", background: "linear-gradient(120deg, color-mix(in srgb, var(--primary) 10%, transparent), color-mix(in srgb, black 40%, var(--background)))", boxShadow: "0 8px 22px color-mix(in srgb, black 24%, transparent)" }}>
    <div style={{ display: "grid", gridTemplateColumns: "10px 1fr auto", gap: "0.8rem", alignItems: "start" }}>
      <div style={{ background: "linear-gradient(180deg, var(--primary), color-mix(in srgb, var(--primary) 45%, white))", minHeight: "100%" }} />
      <div style={{ padding: "0.9rem 0" }}>
        <p style={{ margin: 0, fontSize: "0.76rem", letterSpacing: "0.08em", opacity: 0.72 }}>COMPTIA</p>
        <p style={{ margin: "0.35rem 0 0 0", fontWeight: 700, lineHeight: 1.35, fontSize: "1.08rem", display: "flex", alignItems: "center", gap: "0.45rem" }}>
          <Icon icon="shield" size={15} color="#c084fc" />
          CompTIA Security+
        </p>
      </div>
      <div style={{ padding: "0.9rem 0.9rem 0.9rem 0", textAlign: "right" }}>
        <span style={{ display: "inline-block", padding: "0.25rem 0.55rem", borderRadius: "999px", border: "1px solid color-mix(in srgb, var(--primary) 45%, var(--border))", fontSize: "0.76rem", fontWeight: 700 }}>09 Jan 2026</span>
      </div>
    </div>
  </div>

<div
  style={{
    border: "1px solid var(--border)",
    borderRadius: "14px",
    overflow: "hidden",
    background:
      "linear-gradient(120deg, color-mix(in srgb, var(--info) 10%, transparent), color-mix(in srgb, black 40%, var(--background)))",
    boxShadow: "0 8px 22px color-mix(in srgb, black 24%, transparent)",
  }}
>
  <div
    style={{
      display: "grid",
      gridTemplateColumns: "10px 1fr auto",
      gap: "0.8rem",
      alignItems: "start",
    }}
  >
    <div
      style={{
        background:
          "linear-gradient(180deg, var(--info), color-mix(in srgb, var(--info) 45%, white))",
        minHeight: "100%",
      }}
    />
    <div style={{ padding: "0.9rem 0" }}>
      <p
        style={{
          margin: 0,
          fontSize: "0.76rem",
          letterSpacing: "0.08em",
          opacity: 0.72,
        }}
      >
        CISCO NETWORKING ACADEMY
      </p>
      <p
        style={{
          margin: "0.35rem 0 0 0",
          fontWeight: 700,
          lineHeight: 1.35,
          fontSize: "1.08rem",
          display: "flex",
          alignItems: "center",
          gap: "0.45rem",
        }}
      >
        <Icon icon="magnifying-glass" size={15} color="#c084fc" />
        CyberOps Associate
      </p>
    </div>
    <div style={{ padding: "0.9rem 0.9rem 0.9rem 0", textAlign: "right" }}>
      <span
        style={{
          display: "inline-block",
          padding: "0.25rem 0.55rem",
          borderRadius: "999px",
          border:
            "1px solid color-mix(in srgb, var(--info) 45%, var(--border))",
          fontSize: "0.76rem",
          fontWeight: 700,
        }}
      >
        09 Feb 2024
      </span>
    </div>
  </div>
</div>

<div
  style={{
    border: "1px solid var(--border)",
    borderRadius: "14px",
    overflow: "hidden",
    background:
      "linear-gradient(120deg, color-mix(in srgb, var(--success) 10%, transparent), color-mix(in srgb, black 40%, var(--background)))",
    boxShadow: "0 8px 22px color-mix(in srgb, black 24%, transparent)",
  }}
>
  <div
    style={{
      display: "grid",
      gridTemplateColumns: "10px 1fr auto",
      gap: "0.8rem",
      alignItems: "start",
    }}
  >
    <div
      style={{
        background:
          "linear-gradient(180deg, var(--success), color-mix(in srgb, var(--success) 45%, white))",
        minHeight: "100%",
      }}
    />
    <div style={{ padding: "0.9rem 0" }}>
      <p
        style={{
          margin: 0,
          fontSize: "0.76rem",
          letterSpacing: "0.08em",
          opacity: 0.72,
        }}
      >
        CISCO NETWORKING ACADEMY
      </p>
      <p
        style={{
          margin: "0.35rem 0 0 0",
          fontWeight: 700,
          lineHeight: 1.35,
          fontSize: "1.08rem",
          display: "flex",
          alignItems: "center",
          gap: "0.45rem",
        }}
      >
        <Icon icon="terminal" size={15} color="#c084fc" />
        DevNet Associate
      </p>
    </div>
    <div style={{ padding: "0.9rem 0.9rem 0.9rem 0", textAlign: "right" }}>
      <span
        style={{
          display: "inline-block",
          padding: "0.25rem 0.55rem",
          borderRadius: "999px",
          border:
            "1px solid color-mix(in srgb, var(--success) 45%, var(--border))",
          fontSize: "0.76rem",
          fontWeight: 700,
        }}
      >
        01 Feb 2024
      </span>
    </div>
  </div>
</div>

  <div style={{ border: "1px solid var(--border)", borderRadius: "14px", overflow: "hidden", background: "linear-gradient(120deg, color-mix(in srgb, var(--warning) 10%, transparent), color-mix(in srgb, black 40%, var(--background)))", boxShadow: "0 8px 22px color-mix(in srgb, black 24%, transparent)" }}>
    <div style={{ display: "grid", gridTemplateColumns: "10px 1fr auto", gap: "0.8rem", alignItems: "start" }}>
      <div style={{ background: "linear-gradient(180deg, var(--warning), color-mix(in srgb, var(--warning) 45%, white))", minHeight: "100%" }} />
      <div style={{ padding: "0.9rem 0" }}>
        <p style={{ margin: 0, fontSize: "0.76rem", letterSpacing: "0.08em", opacity: 0.72 }}>CISCO NETWORKING ACADEMY</p>
        <p style={{ margin: "0.35rem 0 0 0", fontWeight: 700, lineHeight: 1.35, fontSize: "1.08rem", display: "flex", alignItems: "center", gap: "0.45rem" }}>
          <Icon icon="network-wired" size={15} color="#c084fc" />
          CCNAv7: Enterprise Networking, Security, and Automation
        </p>
      </div>
      <div style={{ padding: "0.9rem 0.9rem 0.9rem 0", textAlign: "right" }}>
        <span style={{ display: "inline-block", padding: "0.25rem 0.55rem", borderRadius: "999px", border: "1px solid color-mix(in srgb, var(--warning) 45%, var(--border))", fontSize: "0.76rem", fontWeight: 700 }}>27 Jan 2022</span>
      </div>
    </div>
  </div>
</div>
