---
title: "In Progress"
description: "Certifications I am currently preparing for."
---

## Active Certifications

<div style={{ display: "flex", flexDirection: "column", gap: "0.9rem" }}>
  <div style={{ border: "1px solid var(--border)", borderRadius: "14px", overflow: "hidden", background: "linear-gradient(120deg, color-mix(in srgb, var(--info) 10%, transparent), color-mix(in srgb, black 40%, var(--background)))", boxShadow: "0 8px 22px color-mix(in srgb, black 24%, transparent)" }}>
    <div style={{ display: "grid", gridTemplateColumns: "10px 1fr auto", gap: "0.8rem", alignItems: "start" }}>
      <div style={{ background: "linear-gradient(180deg, var(--info), color-mix(in srgb, var(--info) 45%, white))", minHeight: "100%" }} />
      <div style={{ padding: "0.9rem 0" }}>
        <p style={{ margin: 0, fontSize: "0.76rem", letterSpacing: "0.08em", opacity: 0.72 }}>PORTSWIGGER</p>
        <p style={{ margin: "0.35rem 0 0 0", fontWeight: 700, lineHeight: 1.35, fontSize: "1.08rem", display: "flex", alignItems: "center", gap: "0.45rem" }}>
          <Icon icon="bug" size={15} color="#c084fc" />
          Burp Suite Certified Practitioner
        </p>
      </div>
      <div style={{ padding: "0.9rem 0.9rem 0.9rem 0", textAlign: "right" }}>
        <span style={{ display: "inline-block", padding: "0.25rem 0.55rem", borderRadius: "999px", border: "1px solid color-mix(in srgb, var(--info) 45%, var(--border))", fontSize: "0.76rem", fontWeight: 700 }}>IN PROGRESS</span>
      </div>
    </div>
  </div>

<div
  style={{
    border: "1px solid var(--border)",
    borderRadius: "14px",
    overflow: "hidden",
    background:
      "linear-gradient(120deg, color-mix(in srgb, var(--success) 10%, transparent), color-mix(in srgb, black 40%, var(--background)))",
    boxShadow: "0 8px 22px color-mix(in srgb, black 24%, transparent)",
  }}
>
  <div
    style={{
      display: "grid",
      gridTemplateColumns: "10px 1fr auto",
      gap: "0.8rem",
      alignItems: "start",
    }}
  >
    <div
      style={{
        background:
          "linear-gradient(180deg, var(--success), color-mix(in srgb, var(--success) 45%, white))",
        minHeight: "100%",
      }}
    />
    <div style={{ padding: "0.9rem 0" }}>
      <p
        style={{
          margin: 0,
          fontSize: "0.76rem",
          letterSpacing: "0.08em",
          opacity: 0.72,
        }}
      >
        HACK THE BOX ACADEMY
      </p>
      <p
        style={{
          margin: "0.35rem 0 0 0",
          fontWeight: 700,
          lineHeight: 1.35,
          fontSize: "1.08rem",
          display: "flex",
          alignItems: "center",
          gap: "0.45rem",
        }}
      >
        <Icon icon="shield" size={15} color="#c084fc" />
        CJCA (Certified Junior Cybersecurity Associate)
      </p>
    </div>
    <div style={{ padding: "0.9rem 0.9rem 0.9rem 0", textAlign: "right" }}>
      <span
        style={{
          display: "inline-block",
          padding: "0.25rem 0.55rem",
          borderRadius: "999px",
          border:
            "1px solid color-mix(in srgb, var(--success) 45%, var(--border))",
          fontSize: "0.76rem",
          fontWeight: 700,
        }}
      >
        IN PROGRESS
      </span>
    </div>
  </div>
</div>

<div
  style={{
    border: "1px solid var(--border)",
    borderRadius: "14px",
    overflow: "hidden",
    background:
      "linear-gradient(120deg, color-mix(in srgb, var(--warning) 10%, transparent), color-mix(in srgb, black 40%, var(--background)))",
    boxShadow: "0 8px 22px color-mix(in srgb, black 24%, transparent)",
  }}
>
  <div
    style={{
      display: "grid",
      gridTemplateColumns: "10px 1fr auto",
      gap: "0.8rem",
      alignItems: "start",
    }}
  >
    <div
      style={{
        background:
          "linear-gradient(180deg, var(--warning), color-mix(in srgb, var(--warning) 45%, white))",
        minHeight: "100%",
      }}
    />
    <div style={{ padding: "0.9rem 0" }}>
      <p
        style={{
          margin: 0,
          fontSize: "0.76rem",
          letterSpacing: "0.08em",
          opacity: 0.72,
        }}
      >
        HACK THE BOX ACADEMY
      </p>
      <p
        style={{
          margin: "0.35rem 0 0 0",
          fontWeight: 700,
          lineHeight: 1.35,
          fontSize: "1.08rem",
          display: "flex",
          alignItems: "center",
          gap: "0.45rem",
        }}
      >
        <Icon icon="spider" size={15} color="#c084fc" />
        CWES (Certified Web Exploitation Specialist)
      </p>
    </div>
    <div style={{ padding: "0.9rem 0.9rem 0.9rem 0", textAlign: "right" }}>
      <span
        style={{
          display: "inline-block",
          padding: "0.25rem 0.55rem",
          borderRadius: "999px",
          border:
            "1px solid color-mix(in srgb, var(--warning) 45%, var(--border))",
          fontSize: "0.76rem",
          fontWeight: 700,
        }}
      >
        IN PROGRESS
      </span>
    </div>
  </div>
</div>

  <div style={{ border: "1px solid var(--border)", borderRadius: "14px", overflow: "hidden", background: "linear-gradient(120deg, color-mix(in srgb, var(--primary) 10%, transparent), color-mix(in srgb, black 40%, var(--background)))", boxShadow: "0 8px 22px color-mix(in srgb, black 24%, transparent)" }}>
    <div style={{ display: "grid", gridTemplateColumns: "10px 1fr auto", gap: "0.8rem", alignItems: "start" }}>
      <div style={{ background: "linear-gradient(180deg, var(--primary), color-mix(in srgb, var(--primary) 45%, white))", minHeight: "100%" }} />
      <div style={{ padding: "0.9rem 0" }}>
        <p style={{ margin: 0, fontSize: "0.76rem", letterSpacing: "0.08em", opacity: 0.72 }}>CROWDSTRIKE UNIVERSITY</p>
        <p style={{ margin: "0.35rem 0 0 0", fontWeight: 700, lineHeight: 1.35, fontSize: "1.08rem", display: "flex", alignItems: "center", gap: "0.45rem" }}>
          <Icon icon="shield" size={15} color="#c084fc" />
          CCFA (CrowdStrike Certified Falcon Administrator)
        </p>
      </div>
      <div style={{ padding: "0.9rem 0.9rem 0.9rem 0", textAlign: "right" }}>
        <span style={{ display: "inline-block", padding: "0.25rem 0.55rem", borderRadius: "999px", border: "1px solid color-mix(in srgb, var(--primary) 45%, var(--border))", fontSize: "0.76rem", fontWeight: 700 }}>IN PROGRESS</span>
      </div>
    </div>
  </div>
</div>
